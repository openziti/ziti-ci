before_install:
  - "[ -z \"$ZITI_CI_VERSION\" ] && go get github.com/netfoundry/ziti-ci@e09225cf27328e69cbbed822df909a2c3567d87a || true"
  - "[ ! -z \"$ZITI_CI_VERSION\" ] && go get github.com/netfoundry/ziti-ci@$ZITI_CI_VERSION || true"
  - echo $(which ziti-ci)
  - ziti-ci version
